export declare const appConfig: (() => {
    port: number;
    host: string;
    version: string;
    environment: string;
}) & import("@nestjs/config").ConfigFactoryKeyHost<{
    port: number;
    host: string;
    version: string;
    environment: string;
}>;
