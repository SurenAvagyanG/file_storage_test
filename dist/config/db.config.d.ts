export declare const dbConfig: (() => {
    host: string;
    port: number;
    database: string;
}) & import("@nestjs/config").ConfigFactoryKeyHost<{
    host: string;
    port: number;
    database: string;
}>;
